# Third-party notices

## Ultralytics

The optional deep pipeline uses Ultralytics YOLOv8n and YOLOv8n-seg. Weights are downloaded on demand and are not committed. See the upstream [repository and license](https://github.com/ultralytics/ultralytics) and [model documentation](https://docs.ultralytics.com/models/yolov8/).

The photograph `data/bus.jpg` comes from:
https://github.com/ultralytics/ultralytics/blob/main/ultralytics/assets/bus.jpg

The repository distributes Ultralytics under AGPL-3.0, with separate commercial licensing offered upstream. Preserve upstream attribution and consult the upstream terms when reusing that software or media. This project does not claim ownership of the photograph or pretrained weights.

`data/deep_smoke_test.mp4`, deep-mode output videos and representative deep images are generated from that photograph. The short input clip applies a known translation to a still image; it is not recorded natural-motion footage and has no ground-truth detection annotations.

## Generated benchmark

`data/sample_shapes.mp4` and `data/ground_truth.csv` are generated by this project's `generate_demo.py` with seed 42. The classical output images, charts and video are execution artifacts of that benchmark.

## Other dependencies

OpenCV, NumPy, Pandas, Matplotlib, Streamlit, Jupyter, ReportLab and their dependencies retain their respective upstream licenses. FFmpeg is an external executable; it is not redistributed here.
